import { ObjectCompareHelper } from './object-compare-helper.helper';

describe('ObjectCompareHelper', () => {
  it('should create an instance', () => {
    expect(new ObjectCompareHelper()).toBeTruthy();
  });
});
