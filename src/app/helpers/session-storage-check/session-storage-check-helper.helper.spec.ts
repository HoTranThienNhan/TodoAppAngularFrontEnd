import { SessionStorageCheckHelper } from './session-storage-check-helper.helper';

describe('SessionStorageCheckHelper', () => {
  it('should create an instance', () => {
    expect(new SessionStorageCheckHelper()).toBeTruthy();
  });
});
