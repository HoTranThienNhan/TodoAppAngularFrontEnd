import { RefreshTokenResDto } from './refresh-token-res-dto.model';

describe('RefreshTokenResDto', () => {
  it('should create an instance', () => {
    expect(new RefreshTokenResDto()).toBeTruthy();
  });
});
